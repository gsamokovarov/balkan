<%# locals: (proposal:) %>

<%= turbo_frame_tag "proposal" do %>
  <%= marketing_form model: proposal, url: (proposal.access_url unless proposal.new_record?) do |f| %>
    <%= f.text_input :name, required: true %>
    <%= f.email_input :email, required: true %>
    <%= f.text_area_input :bio %>
    <%= f.text_input :company %>
    <%= f.text_input :location %>
    <%= f.text_input :social_url %>
    <%= f.text_input :github_url %>
    <%= f.text_input :title, required: true %>
    <%= f.text_area_input :description %>
    <%= f.text_area_input :notes %>

    <%= f.captcha if proposal.new_record? %>

    <%= f.submit(proposal.new_record? ? "Submit" : "Update") %>
  <% end %>
<% end %>
