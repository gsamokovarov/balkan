<%# locals: (urls:) %>

<div class="flex flex-col md:flex-row gap-4">
  <% Array(urls).each do |url| %>
    <iframe src="<%= url %>" class="w-full h-[656px] border-2 border-gray-300 dark:border-gray-600 rounded-md"></iframe>
  <% end %>
</div>
