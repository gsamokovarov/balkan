<%# locals: (id:, title:) %>

<dialog id="<%= id %>" data-controller="modal" class="rounded-lg shadow-xl w-full max-w-sm max-h-[85%] p-0 backdrop:bg-gray-500/75 bg-white dark:bg-gray-800">
  <div class="p-6 space-y-6">
    <div class="flex items-center justify-between">
      <h3 class="text-lg font-medium text-gray-900 dark:text-gray-100"><%= title %></h3>

      <button type="button" data-action="modal#close" class="text-gray-400 dark:text-gray-500 hover:text-gray-500 dark:hover:text-gray-400">
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>

    <%= yield %>
  </div>
</dialog>
