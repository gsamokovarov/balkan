<%# locals: (subscribers:) %>

<%= admin_table subscribers do
  column "#",   -> { it.id }
  column :email
  column :date, -> { it.created_at.to_fs :long }

  column do
    admin_button(:danger, :small, delete: [:admin, it.event, it, page: params[:page]]) { "Delete" }
  end
end %>
