<%# locals: (nav_links:, logo_partial:) %>

<header class="hidden sm:block pt-5">
  <%= render "container" do %>
    <div class="flex items-center justify-between gap-8 mb-16">
      <div class="shrink-0">
        <%= render logo_partial %>
      </div>

      <nav class="flex items-center gap-8 text-lg font-medium">
        <%= render "navigation_links", nav_links: %>
      </nav>
    </div>
  <% end %>
</header>
