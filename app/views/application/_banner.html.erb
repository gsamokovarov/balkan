<% if Current.show_banner? %>
  <div class="flex items-center gap-x-6 bg-banitsa-500 px-6 py-2.5 sm:px-3.5 sm:before:flex-1">
    <p class="text-sm leading-6 text-white truncate">
      <%= link_to blog_path(2) do %>
        <b>Party in Vitamin B starts at April 26th from 19:30.</b>
      <% end %>
    </p>
    <div class="flex flex-1 justify-end">
    </div>
  </div>
<% end %>
