<% title "Sponsorship" %>

<%= render "section", title: "Sponsorship", title_element: "h1", highlight: true do %>
  <%= render "prose", variant: "xl" do %>
    <p>
      By sponsoring Balkan Ruby, you are helping us make a great event while
      promoting your brand to the passionate Ruby developers in Bulgaria, the
      Balkans, and beyond!
    </p>
    <p>
      Check out our sponsorship packages below:
    </p>
  <% end %>

  <% Current.event.sponsorship_packages.each do |package| %>
    <div class="space-y-4">
      <h2 class="text-2xl sm:text-3xl font-extrabold"><%= package.name %></h2>
      <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
        <% package.variants.each do |var|  %>
          <%= render "package", name: var.name, price: (var.price if var.price.positive?), limited: var.quantity do %>
            <%= render_markdown var.perks %>
          <% end %>
        <% end %>
      </div>
      <% if package.description? %>
        <%= render("prose", variant: "xl", class: "text-gray-500") { render_markdown package.description } %>
      <% end %>
    </div>
  <% end %>

  <div class="flex gap-4 flex-wrap">
    <%= link_to "mailto:hi@balkanruby.com?subject=Balkan Ruby sponsorship", class: "btn-primary" do %>
      <svg
        xmlns="http://www.w3.org/2000/svg"
        viewBox="0 0 20 20"
        fill="currentColor"
        class="w-5 h-5"
      >
        <path
          fill-rule="evenodd"
          d="M10 18a8 8 0 100-16 8 8 0 000 16zm.75-11.25a.75.75 0 00-1.5 0v2.5h-2.5a.75.75 0 000 1.5h2.5v2.5a.75.75 0 001.5 0v-2.5h2.5a.75.75 0 000-1.5h-2.5v-2.5z"
          clip-rule="evenodd"
        />
      </svg>
      <span>Sponsor us</span>
    <% end %>
  </div>
<% end %>

<%= render "section", title: "FAQ" do %>
  <%= render "faq" do %>
    <%= render "question", title: "Do I have to provide the banners and roll-up posters?" do %>
      <p>
        If you have existing printed materials, feel free to bring them along.
        Otherwise, our team will be handling everything regarding the banner
        material (printing, setup). Of course, we expect you to provide for the
        artwork (logos, graphics, etc.) that may be needed. The cost of the
        banners is included in the package price.
      </p>
    <% end %>
    <%= render "question", title: "Do I have to provide my own stand for the sponsors hall?" do %>
      <p>
        The exhibit space is a large table (1×2m), provided by us.
      </p>
    <% end %>
    <%= render "question", title: "Do the prices include VAT?" do %>
      <p>
        The prices do not include VAT, which will be added where applicable.
      </p>
    <% end %>
    <%= render "question", title: "Do you have any other sponsorship options?" do %>
      <p>
        We are open to discuss other opportunities and bespoke packages to
        ensure you get the best out of your investment.
      </p>
    <% end %>
  <% end %>
<% end %>

<%= render "tickets/cta", highlight: true %>
