<% if Current.event.sales_tickets? %>
  <div class="grid grid-cols-2 gap-4 sm:flex sm:items-center sm:flex-wrap sm:gap-6">
    <% Current.event.ticket_types.enabled.each do |ticket_type| %>
      <%= render "tickets/ticket_card_link", ticket_type: %>
    <% end %>
  </div>
  <% Current.event.ticket_types.enabled.each do |ticket_type| %>
    <% if ticket_type.description.present? %>
      <%= render "prose" do %>
        <%= render_markdown ticket_type.description %>
      <% end %>
    <% end %>
  <% end %>
<% else %>
  <%= render "tickets/sold_out" %>
<% end %>
