<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap"
      rel="stylesheet"
    >
    <style>
      :root {
        font-size: 1rem;
        line-height: 1.5rem;
        font-family: Inter, ui-sans-serif, system-ui, -apple-system, "system-ui", "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
      }

      .flex { display: flex; }
      .flex-col { flex-direction: column; }
      .flex-wrap { flex-wrap: wrap; }
      .grow { flex-grow: 1; }
      .items-center { align-items: center; }
      .bg-black { background-color: #000; }
      .text-white { color: #fff; }
      .text-white\/70 { rgb(256 256 256/70); }
      .text-black { color: #000; }
      .h-full { height: 100%; }
      .w-56 { width: 14rem; }
      .w-prose { width: 65ch; max-width: 100%; }
      .mx-auto { margin-left: auto; margin-right: auto; }
      .px-4 { padding-left: 1rem; padding-right: 1rem; }
      .py-5 { padding-top: 1.5rem; padding-bottom: 1.5rem; }
      .pb-2 { padding-bottom: 0.5rem; }
      .pb-4 { padding-bottom: 1rem; }
      .py-6 { padding-top: 1.75rem; padding-bottom: 1.75rem; }
      .py-12 { padding-top: 3rem; padding-bottom: 3rem; }
      .py-16 { padding-top: 4rem; padding-bottom: 4rem; }

      .link-secondary { color: #fff; text-decoration: none; }
      .link-secondary:hover { color: #F2ABB2; }

      .link-primary {
        color: #D42133;
        text-decoration-color: #D42133;
        text-decoration-line: underline;
        text-underline-offset: 4px;
      }

      /* Markdown styles */
      .md-prose {
        width: 65ch;
        max-width: 100%;
        color: #000;
        line-height: 1.5;
      }

      .md-prose h1,
      .md-prose h2,
      .md-prose h3,
      .md-prose h4,
      .md-prose h5,
      .md-prose h6 {
        margin-top: 1.5rem;
        margin-bottom: 0.75rem;
        font-weight: 600;
        line-height: 1.25;
        color: #000;
      }

      .md-prose h1 { font-size: 2rem; margin-top: 0; }
      .md-prose h2 { font-size: 1.5rem; }
      .md-prose h3 { font-size: 1.25rem; }
      .md-prose h4 { font-size: 1.125rem; }
      .md-prose h5,
      .md-prose h6 { font-size: 1rem; }

      .md-prose p {
        margin-top: 0;
        margin-bottom: 1rem;
        line-height: 1.5;
      }

      .md-prose a {
        color: #D42133;
        text-decoration: underline;
        text-underline-offset: 2px;
      }

      .md-prose strong,
      .md-prose b {
        font-weight: 600;
      }

      .md-prose em,
      .md-prose i {
        font-style: italic;
      }

      .md-prose ul,
      .md-prose ol {
        margin-top: 0;
        margin-bottom: 1rem;
        padding-left: 2rem;
      }

      .md-prose li {
        margin-bottom: 0.25rem;
      }

      .md-prose ul { list-style-type: disc; }
      .md-prose ol { list-style-type: decimal; }

      .md-prose hr {
        border: 0;
        border-top: 1px solid #e5e7eb;
        margin-top: 2rem;
        margin-bottom: 2rem;
      }

      .md-prose blockquote {
        margin: 1rem 0;
        padding-left: 1rem;
        border-left: 3px solid #D42133;
        color: #6b7280;
        font-style: italic;
      }

      .md-prose code {
        background-color: #f3f4f6;
        padding: 0.125rem 0.25rem;
        border-radius: 0.25rem;
        font-size: 0.875rem;
        font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      }

      .md-prose pre {
        background-color: #f3f4f6;
        padding: 1rem;
        border-radius: 0.375rem;
        overflow-x: auto;
        margin-bottom: 1rem;
      }

      .md-prose pre code {
        background-color: transparent;
        padding: 0;
      }
    </style>
  </head>

  <body class="antialiased text-black">
    <div class="flex flex-col">
      <% if attachments['logo.png'] %>
        <header class="py-6 px-4">
          <%= image_tag attachments['logo.png'].url, title: "Logo", alt: "Logo" %>
        </header>
      <% end %>

      <div class="grow">
        <div class="mx-auto px-4">
          <main class="grow">
            <%= yield %>
          </main>
        </div>
      </div>
    </div>
  </body>
</html>
